service:
  express-app:
    bulid:
      context: .
      dockerfile: dockerfile
    container_name: express_static_server_helloWorld
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    restart: always
